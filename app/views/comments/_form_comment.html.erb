<%= form_for @post.comments.build, remote: true do |f| %>
  <%= hidden_field_tag :micropost_id, @post.id %>
  <%= f.text_area :content, row: 2, class: 'w-100' %><br>
  <%= f.submit 'Create post',  class: 'btn btn-primary' %>
<% end %>
